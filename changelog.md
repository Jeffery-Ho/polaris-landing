# Changelog

## 2026-09-09

- 下载：将本地 ZIP 下载后的文件名改为 `Polaris-AI-0.48.3-build-196.zip`，保留原始资源地址和下载埋点兼容性。
- 修复：Header 的 Download/下载 按钮改为纯色内底，主题渐变仅保留在边框与文字，移动端菜单继续保持无背景。
- 文案：将 Header 主按钮从“Install/安装”改为“Download/下载”，继续指向安装区域。
- 视觉：将本地安装标题旁的推荐 Badge 改为深浅主题自适应的黑白样式，去除渐变边框。
- 视觉：在本地安装标题右侧新增地区推荐 Badge，中文显示“中国大陆推荐”，英文显示“Recommended in Mainland China”。
- 国际化：中文 Footer 保留 “Made with” 英文表述，其余 Footer 文案继续本地化。
- 修复：安装按钮改为纯色内底，主题渐变仅保留在边框与文字，hover 不再填充渐变背景。

- 视觉：统一 Header、Hero 和安装区产品按钮为主题渐变文字与边框，hover 时仅使用纯色内底，不填充渐变背景。
- 排版：精简中文 Hero 标题并固定为两行，收紧中文专用字间距，避免单个汉字换行。
- 视觉：统一按钮为大圆角胶囊样式，保留按钮内部纯色和主题渐变边框/文字规则。
- 视觉：Hero banner 按钮恢复黑白风格，hover 使用黑白反转，不再显示主题渐变背景。
- 交互：统一 Header 和 Hero 锚点按钮的平滑滚动行为，并在开启减弱动画时自动使用无动画跳转。

## 2026-09-08

- 国际化：补齐简体中文能力区的 Chapter View 翻译，避免中文界面残留英文标题。
- 交互：移动端菜单取消当前安装项的白色选中背景和内部边框，保留菜单容器边界与键盘焦点提示。
- 视觉：将 Logo 粉紫强调色升级为橙、粉、蓝的主题渐变，仅用于编号、链接和交互强调状态。
- 主题：新增深色/浅色模式切换，首次访问跟随系统偏好，用户选择写入本地存储并在刷新后保持。
- 视觉：参考 Voyager 将语言控制改为导航外层的图标下拉菜单，并保留独立的胶囊主题开关。
- 修复：提高浅色模式下菜单入口 hover、focus 和展开状态的背景及图标对比度。
- 交互：统一导航、语言、主题、安装、视频、支持和页脚控件的 hover 反馈；焦点环改为深色白环、浅色黑环，并保留 ZIP 安装按钮的主题渐变样式。
- 交互：菜单按钮改为无边框图标，并增加三横线到关闭符号及菜单面板的过渡动效，同时使用随主题切换的高对比度焦点环。
- 设计：第二轮对齐 Vercel 的布局语言，将 Hero 改为左右分栏，能力区改为网格分隔，并统一使用低圆角、细边框和无渐变表面。
- 设计：移动端 Header 收敛为 Polaris Logo 加菜单图标，展开后显示安装、功能、GitHub 和语言切换，并支持 Escape 关闭与焦点回收。
- 设计：参考 Vercel 的极简信息层级，重构 landing page 为“产品价值、介绍视频、能力说明、安装选择、支持与隐私”的叙事结构，并改用黑色背景、白色文字、细边框和分层 CTA。
- 设计：保留 PayPal 支持入口的高对比橙色按钮，同时将链接、焦点和能力编号统一为 Logo 边框的粉紫色。
- 体验：顶部 Download 改为平滑滚动到安装区域，并将本地安装说明简化为四步操作。
- 体验：点击本地 ZIP 下载后显示短暂 loading 状态，避免重复点击并保留原生下载行为。
- 功能：接入阿里云 SLS WebTracking，记录 UTM 到达 landing page 和本地 ZIP 下载事件，并通过 STS 临时凭证发送。
- 部署：完成 `cn-hangzhou` 生产 SLS、RAM 和 FC 配置，并将 STS 接口地址写入 landing page。
- 功能：新增 Polaris 本地 ZIP 安装和 Chrome Web Store 安装入口，并提供四步本地安装说明。
- 功能：landing homepage 支持英语和简体中文切换，首次访问跟随浏览器语言，用户选择保存到浏览器本地。
- 设计：参考 Readmate 的宽版透明导航，将 Download 设为 header 主按钮，保留其他入口为次级链接。
- 资源：新增当前 `Polaris-for-Web-0.48.3-build-196.zip` 离线安装包。
- 部署：新增 GitHub Actions Pages 部署流程，`main` 分支更新后自动发布 landing page。
